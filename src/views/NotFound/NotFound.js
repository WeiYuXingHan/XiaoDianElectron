import style from "./NotFound.module.css";

export default function NotFound() {
  return (
    <section aria-label="not found" className={style.wrapper}>
      <h1>404 | NotFound</h1>
    </section>
  );
}
